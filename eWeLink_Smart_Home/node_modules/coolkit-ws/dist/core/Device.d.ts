import { IDeviceConfig, IConfig } from '../ts/interfaces';
export default class Device {
    static wsConfig: IConfig;
    constructor(wsConfig: IConfig);
    sendThing(deviceConfig: IDeviceConfig, action: 'query' | 'update'): Promise<{
        action: "update" | "query";
        deviceid: string;
        apikey: string;
        userAgent: string;
        sequence: number;
        params: any;
    }>;
}
